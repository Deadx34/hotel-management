import React from 'react';
import Login from '../components/Auth/Login'; // Import the actual login form component

const LoginPage = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default LoginPage;